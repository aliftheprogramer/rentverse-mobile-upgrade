import 'package:flutter/material.dart';

class TenantRentPage extends StatelessWidget {
  const TenantRentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tenant Rent'));
  }
}

import 'package:flutter/material.dart';

class TenantChatPage extends StatelessWidget {
  const TenantChatPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tenant Chat'));
  }
}
